# ─── Prime My Brain — Server Environment ─────────────────────
# Used by: task dev:server  (PocketBase Go library mode)
# Source: main.go reads these via getEnv()

# ── PocketBase ──
PB_ADMIN_EMAIL=admin@example.com
PB_ADMIN_PASSWORD=changeme123
PB_DATA_DIR=./pb_data
PB_PORT=8090
PB_AUTO_MIGRATE=true

# ── LINE Login ──
# LINE_CLIENT_ID is required for ID token verification.
# Get it from: https://developers.line.biz/console/
LINE_CLIENT_ID=2010098206
