# ─── Prime My Brain — Root Environment ────────────────────────
# Loaded by Taskfile globally. App-specific env files:
#   server/.env      — PocketBase + LINE config (task dev:server)
#   client/apps/*/.env — Vite client env (VITE_ prefixed)
